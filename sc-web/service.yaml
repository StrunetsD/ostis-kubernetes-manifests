apiVersion: v1
kind: Service
metadata:
  name: sc-web-service
  namespace: ostis-system
spec:
  selector:
    app: sc-web
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    nodePort: 30080  # ✅ ДОБАВИЛ nodePort для внешнего доступа
  type: NodePort  # ✅ ИЗМЕНИЛ с ClusterIP на NodePort